<%= form_for(@fundamental) do |f| %>
  <% if @fundamental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fundamental.errors.count, "error") %> prohibited this fundamental from being saved:</h2>

      <ul>
      <% @fundamental.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
